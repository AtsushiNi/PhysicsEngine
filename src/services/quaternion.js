export default class Quaternion {
  constructor(value) {
    this.value = value
  }

  // クォータニオンのクラス
  // this.valueは配列[w, x, y, z]を格納している

  // このクォータニオンを返す
  // return Quaternion
  getValue = () => {
    this.value
  }
}
